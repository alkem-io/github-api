Update the various packages
- try an alternative to xlsx?

Ensure that the column order is fixed in the generated spreadsheet

Map the "Quartile" column to a set of fixed values that are displayable

Update the logic in the spreadsheet to pick up the right columns